#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    listcpy = []
    for i in listcpy:
        if i % 2 == 0:
            listcpy.append(True)
        else:
            listcpy.append(False)
    return (listcpy)
